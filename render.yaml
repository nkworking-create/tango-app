services:
  - type: web
    name: tango-app
    runtime: node
    buildCommand: npm install --ignore-scripts
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
